name: Deploy to Kubernetes

on:
  workflow_run:
    workflows: ["Build & Push Docker image (GHCR)"]
    types:
      - completed
  workflow_dispatch:

jobs:
  deploy:
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    runs-on: self-hosted

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set image tag
        run: |
          # если деплой запущен из workflow_run, берем branch/tag из workflow_run
          # при пуше в main будет latest
          # при пуше тега vX.Y.Z будет vX.Y.Z
          REF="${{ github.ref }}"
          if [[ "${{ github.event_name }}" == "workflow_run" ]]; then
            REF="${{ github.event.workflow_run.head_branch }}"
          fi

          # Пытаемся понять: это тег v* или обычный main
          # В workflow_run tag напрямую не всегда доступен, поэтому делаем просто:
          # main -> latest
          echo "TAG=latest" >> $GITHUB_ENV

      - name: Install kubectl
        run: |
          curl -LO https://dl.k8s.io/release/v1.29.0/bin/linux/amd64/kubectl
          sudo install -o root -g root -m 0755 kubectl /usr/local/bin/kubectl
          kubectl version --client

      - name: Install Helm
        run: |
          curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash
          helm version

      - name: Configure kubeconfig
        run: |
          mkdir -p ~/.kube
          echo "${{ secrets.KUBECONFIG_B64 }}" | base64 -d > ~/.kube/config
          chmod 600 ~/.kube/config
          kubectl cluster-info

      - name: Deploy via Helm
        run: |
          helm upgrade --install testapp ./helm/testapp \
            -n testapp --create-namespace \
            --set image.repository=ghcr.io
